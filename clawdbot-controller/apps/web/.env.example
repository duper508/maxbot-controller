# Environment Configuration for ClawdBot Controller Web App
# Copy this file to .env.local and fill in actual values
# 
# ⚠️  SECURITY NOTICE:
# - NEVER commit .env files to Git
# - Only .env.example (without secrets) should be in repo
# - Add .env.local, .env, .env.*.local to .gitignore

# ============================================================================
# REQUIRED: NEXTAUTH CONFIGURATION - GitHub OAuth
# ============================================================================
# These are your GitHub OAuth application credentials
# Get them from: https://github.com/settings/developers

# GitHub OAuth Client ID (public, can expose in code)
GITHUB_ID=your_github_client_id_here

# GitHub OAuth Client Secret (SERVER-ONLY, NEVER expose to client)
# ⚠️ KEEP THIS SECRET - NEVER COMMIT OR EXPOSE
GITHUB_SECRET=your_github_client_secret_here

# GitHub Personal Access Token (for collaborator verification)
# Create at: https://github.com/settings/tokens
# Required scopes: repo (for private repo access check)
# ⚠️ KEEP THIS SECRET - NEVER COMMIT OR EXPOSE
GITHUB_PAT=ghp_your_personal_access_token_here

# NextAuth.js secret for signing JWTs and encrypting sessions
# Generate with: openssl rand -base64 32
# ⚠️ KEEP THIS SECRET - NEVER COMMIT OR EXPOSE
NEXTAUTH_SECRET=your_nextauth_secret_here_generated_with_openssl

# NextAuth.js callback URL (used in OAuth redirect)
# Local development:
NEXTAUTH_URL=http://localhost:3000

# For Vercel deployment, set to your domain:
# NEXTAUTH_URL=https://your-domain.vercel.app

# ============================================================================
# REQUIRED: DISCORD CONFIGURATION - Webhook & Bot Token
# ============================================================================
# These credentials control Discord webhook and bot operations
# ALL KEPT SERVER-SIDE - NEVER exposed to browser

# Discord Channel ID (where commands/responses appear)
# Get from: Right-click channel in Discord > Copy ID (enable developer mode first)
# This is used for polling responses from the bot
DISCORD_CHANNEL_ID=1472555271166623775

# Discord webhook URL for sending commands to bot
# Format: https://discord.com/api/webhooks/{WEBHOOK_ID}/{WEBHOOK_TOKEN}
# Get from: Server Settings > Integrations > Webhooks > Create
# ⚠️ KEEP THIS SECRET - NEVER COMMIT OR EXPOSE
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Discord bot token for polling messages and channel operations
# Get from: Discord Developer Portal > Applications > Your Bot > Token > Copy
# Required permissions: Read Messages, Read Message History
# ⚠️ KEEP THIS SECRET - NEVER COMMIT OR EXPOSE
DISCORD_BOT_TOKEN=MzA0MzE3YTJlMGI4NDAwMz... (your bot token here)

# ============================================================================
# PUBLIC API CONFIGURATION
# ============================================================================
# These are SAFE to expose to the browser (use NEXT_PUBLIC_ prefix)
# DO NOT put any secrets here!

# Base URL for API calls (used by client JavaScript)
# Local development:
NEXT_PUBLIC_API_BASE=http://localhost:3000/api

# For Vercel deployment, set to your domain:
# NEXT_PUBLIC_API_BASE=https://your-domain.vercel.app/api

# ============================================================================
# OPTIONAL: Logging & Debugging
# ============================================================================

# Log level: debug, info, warn, error
# LOG_LEVEL=info

# Enable debug logging for specific modules
# DEBUG=clawdbot:*

# Node environment (usually set by deployment platform)
NODE_ENV=development

# ============================================================================
# OPTIONAL: Analytics & Monitoring
# ============================================================================

# Optional: Sentry error tracking
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Optional: Analytics service
# NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id_here

# ============================================================================
# VERCEL DEPLOYMENT NOTES
# ============================================================================
#
# When deploying to Vercel:
# 1. Go to Project Settings > Environment Variables
# 2. Add all variables with REQUIRED label below (from this file)
# 3. For each secret, click "Add to Production/Preview/Development" as needed
#
# REQUIRED for Vercel:
#   - GITHUB_ID
#   - GITHUB_SECRET
#   - GITHUB_PAT
#   - NEXTAUTH_SECRET
#   - NEXTAUTH_URL (set to your Vercel domain)
#   - DISCORD_WEBHOOK_URL
#   - DISCORD_BOT_TOKEN
#   - DISCORD_CHANNEL_ID
#
# Make sure NEXTAUTH_URL matches your Vercel domain!
# Example: https://clawdbot-controller.vercel.app
