{
  "version": 2,
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "apps/web/pages/api/$1.ts" },
    { "src": "/(.*)", "dest": "apps/web/pages/$1" }
  ]
}
